using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using GGJ23;

public class GenerateMap : MonoBehaviour
{
    /*
    // Start is called before the first frame update
    RootGenerator _rootGenerator;
    PropsSpawner _propsSpawner;
    

    void Start()
    {
        _rootGenerator = GetComponent<RootGenerator>();
        _propsSpawner = GetComponent<PropsSpawner>();

        _rootGenerator.GetMeshGenerator().OnMeshGenerated += SpawnProps;

        _rootGenerator.GenMesh();

    }

    void SpawnProps()
    {

        _propsSpawner.SetGround(gameObject);
        _propsSpawner.Spawn();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
